;;; General preferences that I want to persist so don't do through the customize interface

(setq truncate-lines t)
(setq ring-bell-function 'ignore)

(global-set-key (kbd "C-x C-f") 'find-file)

(provide 'init-preferences)
